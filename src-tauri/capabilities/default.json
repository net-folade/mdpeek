{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "mdpeek needs a native file picker, the ability to hand external links to the system browser, Finder reveal for local files it cannot display, and window dragging for its overlay title bar. All filesystem reads go through this app's own commands, not the fs plugin.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:allow-open",
    {
      "identifier": "opener:allow-open-url",
      "allow": [
        { "url": "https://*" },
        { "url": "https://**" },
        { "url": "http://*" },
        { "url": "http://**" },
        { "url": "mailto:*" }
      ]
    },
    "opener:allow-reveal-item-in-dir"
  ]
}
