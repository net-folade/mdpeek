{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "mdpeek needs only a native file picker and the ability to hand external links to the system browser. All filesystem reads go through this app's own commands, not the fs plugin.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    {
      "identifier": "opener:allow-open-url",
      "allow": [{ "url": "https://*" }, { "url": "https://**" }, { "url": "http://*" }, { "url": "http://**" }]
    }
  ]
}
